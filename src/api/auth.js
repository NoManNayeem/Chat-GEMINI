
// This is a dummy authentication module.
// Replace with actual authentication logic.

const authenticate = (username, password) => {
  return username === 'Nayeem' && password === 'password';
};

export default authenticate;
